import clsx from "clsx";
import React from "react";

const SalesChart = ({ className }) => {
  return (
    <div className={clsx("bg-slate-900", className)}>
      <p>define</p>
    </div>
  );
};

export default SalesChart;
